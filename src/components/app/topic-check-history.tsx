"use client";

import type { TopicCheckEntry } from "./study-state";
import { subjectThemeStyle } from "./subjects";

export function TopicCheckHistory({ subjectId, entries }: { subjectId: string; entries: TopicCheckEntry[] }) {
  return <section style={subjectThemeStyle(subjectId)} className="mx-auto max-w-[1060px] px-4 pb-12 sm:px-6"><div className="rounded-2xl border border-amber-100 bg-white px-5 py-5 shadow-[0_14px_38px_-34px_rgba(217,119,6,.5)] dark:border-amber-900 dark:bg-slate-900"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-heading m-0 text-lg font-semibold text-gray-900 dark:text-white">Topic Check history</h2><p className="m-0 mt-1 text-[13px] leading-relaxed text-gray-500">Completed checks in this browser session.</p></div><span className="rounded-full bg-amber-50 px-2.5 py-1 text-[12px] font-semibold text-amber-700 dark:bg-amber-400/10 dark:text-amber-200">{entries.length}</span></div>{entries.length ? <div className="mt-4 space-y-2">{entries.map((entry) => <article key={entry.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-amber-100 bg-amber-50/60 px-4 py-3 dark:border-amber-900 dark:bg-amber-400/[.07]"><div><div className="text-[14px] font-semibold text-gray-900 dark:text-white">{entry.topicName}</div><p className="m-0 mt-1 text-[12.5px] text-gray-500">{new Intl.DateTimeFormat(undefined, { day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" }).format(new Date(entry.completedAt))} · {entry.questionCount} questions</p></div><span className={`rounded-full px-2.5 py-1 text-[12px] font-semibold ${entry.status === "independent" ? "bg-emerald-100 text-emerald-800 dark:bg-emerald-400/15 dark:text-emerald-200" : "bg-cyan-100 text-cyan-800 dark:bg-cyan-400/15 dark:text-cyan-200"}`}>{entry.status === "independent" ? "Independent" : `${entry.assistedCount} assisted`}</span></article>)}</div> : <div className="mt-4 rounded-xl border border-dashed border-amber-200 bg-amber-50/40 px-4 py-4 text-[13px] leading-relaxed text-gray-500 dark:border-amber-900 dark:bg-amber-400/[.06]">Complete a Topic Check to build a simple record of the foundations you have practised.</div>}</div></section>;
}
